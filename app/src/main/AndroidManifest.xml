<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pbobaru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logobukuwali"
        android:label="Buku Wali"
        android:roundIcon="@mipmap/logobukuwali"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NilaiSemester"></activity>
        <activity android:name=".NilaiActivity" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pembayaranspp" />
        <activity android:name=".aktivitas" />
        <activity android:name=".datadiri" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>